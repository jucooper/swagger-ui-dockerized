# Swagger UI & Validator *Dockerized*

Spin up your own local copy of Swagger UI and the Swagger Validator all within Docker.

## Usage
1. Run `$ docker-compose up` within the root directory.
2. Visit `http://localhost:3000`
